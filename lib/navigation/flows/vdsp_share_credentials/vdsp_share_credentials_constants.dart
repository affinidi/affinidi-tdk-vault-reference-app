abstract class VdspShareCredentialsRoutePath {
  static const base = '/vdsp-share';
  // static String claimCredentialWithId(String profileId) => '$base/$profileId';
}

abstract class VdspShareCredentialsRouteName {
  static const base = 'vdsp-share';
}
